type: object
description: A workflow Submission
properties:
  name:
    type: string
    description: Name of submission
  submitter:
    type: string
    description: Submitter name
  status:
    $ref: ../definitions/SubmissionStatus.yaml
  workflow:
    type: string
    description: Workflow to run
  workflowInputs:
    type: string
    description: Worklfow inputs
required:
  - name
  - submitter