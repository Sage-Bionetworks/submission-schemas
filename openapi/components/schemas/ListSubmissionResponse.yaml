type: object
description: A page of queues
allOf:
  - $ref: ../schemas/ListResponseMetadata.yaml
  - type: object
    properties:
      submissions:
        description: An array of queues
        type: array
        items:
          - $ref: Submission.yaml
